FROM nginx:alpine

# copy lsg bundle
COPY lsg-public/ /usr/share/nginx/html

ENV CI_COMMIT_SHORT_SHA="${CI_COMMIT_SHORT_SHA}"
ENV CI_COMMIT_REF_NAME="${CI_COMMIT_REF_NAME}"
ENV CI_REPOSITORY_URL="${CI_REPOSITORY_URL}"
ENV CI_PROJECT_PATH="${CI_PROJECT_PATH}"

