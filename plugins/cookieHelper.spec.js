import { getCookie, setCookie, removeCookie } from './cookieHelper'

describe('cookieHelper', () => {
  describe('during interaction', () => {
    test('should get cookie given', () => {})
    test('should set cookie given', () => {})
    test('should remove cookie given', () => {})
  })
})
