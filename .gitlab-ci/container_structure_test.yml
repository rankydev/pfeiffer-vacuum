include:
  - local: '/.gitlab-ci/container_structure_test.template.yml'

.audit_frontend:
  stage: audit
  extends: .container-structure-test
  variables:
    # Basic for git ci runner setup (docker in docker)
    DOCKER_TLS_CERTDIR: ""
    DOCKER_DRIVER: overlay2
    DOCKER_BUILDKIT: 1
  cache: {}
  tags:
    - docker
  script:
    - echo "Run container structure test for image ${DOCKER_IMAGE_NAME}:${CI_COMMIT_REF_SLUG}"
    - docker login -u ${CI_REGISTRY_USER} -p ${CI_REGISTRY_PASSWORD} ${CI_REGISTRY}
    - docker pull ${DOCKER_IMAGE_NAME}:${CI_COMMIT_REF_SLUG}
    - container-structure-test test --image ${DOCKER_IMAGE_NAME}:${CI_COMMIT_REF_SLUG} --config .gitlab-ci/container_structure_test.config.yml

audit-frontend:
  extends: .audit_frontend
  needs: [ "build-docker-image-frontend" ]
  except:
    - develop

audit-frontend-develop:
  extends: .audit_frontend
  needs: [ "build-docker-image-frontend-develop" ]
  only:
    - develop

